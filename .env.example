# .env.example
# Copy to .env and edit values.

# Zammad instance base URL
ZAMMAD_URL=https://your-zammad-instance.com

# Zammad API token
ZAMMAD_TOKEN=put_your_token_here

# Optional: enable scheduler file logging in addition to stdout
# (stdout is what shows up in `docker logs`)
SCHEDULER_LOG_TO_FILE=0
